-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
httpx>=0.27
# P0.2b: the vector index. PINNED so the headline drop-before-search + recall
# tests RUN in CI (not silently skipped via importorskip -> false-green).
turbovec==0.7.0
# P0.2a: SurrealDB python SDK (the unified-store tests).
surrealdb>=2.0
sqlglot>=25
semantic-router>=0.1
opentelemetry-api>=1.20
opentelemetry-sdk>=1.20
# deterministic synthetic-data + dataframe deps (P0.1a seeds).
faker>=40
polars>=1.0
