# CLEARANCE / P0 — Approved Decomposition (14 phases, roadmap-inclusive)

**Date:** 2026-06-08 · **Owner:** Shuri · **Status:** CEO-approved 2026-06-08 · **Repo:** `enterprise-rag-engine`

> Source spec: `docs/superpowers/specs/2026-06-08-clearance-polystore-design.md` · World bible: `docs/meridian-org-and-data.md` · Build philosophy: NO YAGNI; multi-setup paths real+tested; 7 pillars (SOVEREIGN dropped). Ledger surface spec: `marketing/portfolio_app` branch `docs/engineering-ledger-spec`.

Each phase is an increasing-impact increment that ships demoable value; the destination is the full robust system. Roadmap items are pulled in and ordered by impact, never dropped. Each phase ends with the leak-audit oracle green + eval gate passing + L1–L7 per the verification matrix.

## Build order (de-risk earliest)

| Phase | Increment | Expanded-scope / multi-setup notes | Size |
|---|---|---|---|
| **S1 spike** | TurboVec-in-serverless probe | ✅ KEEP verdict (`docs/spikes/s1-turbovec-serverless.md`) | done |
| **P0.1a** Meridian Data Foundation | full reusable data estate | 16 synthetic sources (+payroll/benefits/recruiting/expenses/tax/treasury) + 7 real fixtures + legacy_mart + personas/matrix | L |
| **P0.1b** Catalog + Governance-v2 | catalog registry, SecurityContext→assets/columns, leak oracle bound to matrix | `Connector` ABC, column masking | M |
| **P0.2a** SurrealDB spine | unified store: graph/SurrealQL/full-text/catalog | self-host (titan-pc) AND Cloud both built+tested | M |
| **P0.2b** TurboVec vector index | compressed ANN + vector allowlist pre-filter + offline build — **BUILT + unit-proven, NOT yet wired into live retrieval** | + Qdrant fallback adapter real & tested; recall re-validated on real embeddings (~0.45 tight / ~0.90+ text; reranker load-bearing) | L |
| **P0.2c** Retrieval integration | **DONE** — `TurboVecRetriever` wired into the pipeline (vector mode), **reranker mandatory** on the coarse set, vector queries through the **allowlist pre-filter** + downstream C1; **E2E C1-over-vector** (class-D via live TurboVec → `[REDACTED]`; **hardened** to assert the L5 chunk never enters the candidate set, so it fails if the pre-filter is bypassed). Closed the build-vs-wired gap. | live path uses an in-memory `ChunkSource` (store adapter → P0.2d) | M |
| **P0.2d** Store-backed retrieval + ELASTIC | **DONE** — `SurrealChunkSource` implements the `ChunkSource` protocol over the SurrealDB `chunk` table (`get_chunk` full hydrate; `SecurityContext` from cls/level via shared `_CLASS_ROLES`); **store-backed C1-over-vector E2E** proves live enforcement against a real SurrealDB (class-D → `[REDACTED]`, L5 never in candidate set — hardened). ELASTIC: `all_chunk_security()` now **projects ids+ACLs only** (`SELECT id, asset_id, cls, level`) — no text/vector blobs per query. **Remaining ELASTIC follow-on:** `authorized_chunk_ids` is still O(N) (evaluates every chunk's ACL) — sub-linear ReBAC list-objects (Oso/SpiceDB, spec §9) tracked for a later phase. | both stores live (in-mem + SurrealDB-backed) | M |
| **P0.3a** BigQuery PB tail + SQL safety | **DONE** — `GuardedBigQuery` 4-gate wrapper: `sql/ast_gate` (read-only sqlglot — refuse DML/DDL/injection) + `sql/cost_guard` (mandatory partition filter + `maximum_bytes_billed` via dry-run) + transpile; config-driven cap/partition (CONFIGURABLE). Warehouse assets (NYC TLC/GDELT/TPC-DS) governed like everything else (masked coords/email). All unit tests LOCAL/no-creds (S4-proven contract); live path creds-gated `@pytest.mark.bq`. make ci: 213/0-skip. | tiered parsers (Docling+LlamaParse+Reducto) → P0.3b/ingestion follow-on | M |
| **P0.3b** Text-to-SQL agent | **DONE** — `TextToSqlAgent`: NL → `SqlGenerator` draft → `GuardedBigQuery` (P0.3a gates, can ONLY emit guarded SQL) → **result rows MASKED before the model** (🔴 hard gate: masked column `[REDACTED]`, raw never in answer/citations/generator-input — E2E proven-to-bite) → answer. FakeSqlGenerator (no-creds core) + OpenAICompatGenerator (Groq/NVIDIA live, creds-gated). **SemanticRouter** adopted (semantic-router + deterministic LOCAL HashingEmbedder DenseEncoder, no LLM in hot path) behind QueryRouter ABC; HeuristicRouter fast-path kept. make ci: 249/0-skip. | tiered parsers → ingestion phase | M |
| **P0.4** Opaque-schema glossary | **DONE** — first-party (S3 verdict). `enrichment/schema_glossary/`: profiler (value stats) + view_miner (sqlglot lineage, **PER-TABLE**: `tbl_44.text_2`=name vs `tbl_71.text_2`=status) + drafter (FakeGlossaryDrafter core / LlmGlossaryDrafter live) + build_glossary (mined HIGH > drafted LOW) + drift CI + resolver. 🎯 text-to-SQL retrieves over the **GLOSSARY not the raw schema** (NL "name" → `SELECT text_2 FROM tbl_44`); per-table masking respected; glossary in SurrealDB. make ci 289/0-skip. | built standalone (pipeline-wire follow-on) | L |
| **P0.5** Funnel + observability | **DONE** — `funnel/` (FunnelTrace: stage-by-stage candidate collapse, monotonic non-increasing enforced; compute_funnel heads it with the catalog's STATED PB-tail scale+provenance); scale_badge/provenance_url/row_count surfaced on CatalogAsset; `observability/` (Tracer + InMemorySpanCollector no-creds + OTel emission + LangfuseExporter creds-gated; per-request requestID + cost/token spans); durable `AuditSink` (InMemory + SurrealAuditSink) wired into chunk_source so malformed-row drops are queryable. make ci 320/0-skip. | Langfuse creds-gated; OTel local | S |
| **P0.6** Governance pre-filter ALL modes + sub-linear ReBAC | **DONE** — index-level permission PRE-filter extended from vector to **graph / structured-SQL / lexical**: `SurrealStore.graph_neighbors/structured_rows/fulltext_search` each scope `id IN $allow` at query time (a denied node/row/hit is never a candidate, drop-before-search); `retrieval/{graph,structured,lexical}_mode.py` retrievers derive the per-session allowlist via the swappable backend; hardened tests assert the denied item is absent from each mode's candidate set BEFORE the L5 post-filter, **mutation-proven to bite** (bypass leaks in all 3 modes). **Sub-linear ReBAC allowlist** behind `AllowlistBackend` ABC: `InProcessAllowlist` (default + zero-dep fallback, canonical decision source), `LocalReBACAllowlist` (zero-dep relationship-tuple kernel = the SpiceDB *model*, always-on so parity is provable no-creds), `SpiceDBAllowlist` (authzed LookupResources, binary/creds-gated), `OsoCloudAllowlist` (oso-cloud list, creds-gated). **PARITY GUARD**: every backend == `InProcessAllowlist` across a 7×6 session/chunk matrix spanning every governance leg — mutation-proven (PII-threshold drift + NTK-gate removal each caught as a LEAK). make ci 341/0-skip; 210-cell oracle + leak demos green. | Oso Cloud + SpiceDB self-host both real (gated); LocalReBAC + in-process always tested | L |
| **P0.7** Semantic caching (PERMISSION-AWARE) | **DONE** — permission-aware semantic cache, fail-closed. Headline governance invariant (the P0.7 leak oracle): a cached answer NEVER crosses a permission boundary, defended TWO ways — (1) the cache key namespaces by `AuthScope` (clearance_level + sorted roles) so a different scope can't reach another's entries, and (2) the stored governed payload is RE-GOVERNED for the requesting session on every hit (`govern_fn`; all-denied → fail-closed miss). `cache/`: `key.AuthScope`, `SemanticCache` (cosine match over the deterministic `HashingEmbedder`, no creds; TTL + LRU eviction = ELASTIC bounded footprint), `observability.record_cache_hit` (a hit emits a `cache_lookup` span + funnel note + `AuditSink` `cache_hit` event — TRANSPARENT, NOT bypassing audit; metric-only attrs, no-PII), `SurrealCacheStore` (durable, gated; `invalidate_version` = P0.9 CDC seam). Config: `cache_*` knobs env-driven + `__post_init__` schema-validation refuses to start on invalid (CONFIGURABLE). **Mutation-proven:** scope-blind key → intern hits CFO's entry (in-mem AND durable); no-PII add-content → trail test fails. make ci 379/0-skip; 210-cell oracle + leak demos green. | live Voyage embedder + durable SurrealDB cache gated; in-mem + HashingEmbedder always tested | M |
| **P0.8** Self-RAG + agentic loop (GOVERNANCE-PRESERVING) | **DONE** — corrective grading + a bounded agentic loop, fail-closed. **Headline governance invariant** (the P0.8 leak oracle): the loop is NEVER a permission-escalation path — it NEVER retrieves directly / NEVER builds an allowlist; it calls an injected session-scoped `retrieve_fn(query, session)` (the real allowlist-pre-filtered retriever), reformulates QUERY TEXT only, and threads the FIXED session into every iteration, so a corrective re-retrieval still only sees authorized content (asserted across ALL iterations). `selfrag/`: `grader` (Grader Protocol + deterministic `FakeGrader`: relevance=query-token coverage, groundedness=answer-token support, fail-closed on empty), `loop.CorrectiveLoop` (retrieve→grade-relevance→reformulate/re-retrieve→generate→grade-groundedness→refine/abstain/return; **bounded iterations + grader circuit breaker** = RESILIENT, no infinite-loop/hang; **abstain fail-closed** = no fabrication, stop_reason on provenance; per-iteration span+funnel note = TRANSPARENT), `openai_grader.OpenAICompatGrader` (Groq/NVIDIA, creds-gated, fail-closed JSON parse). Config `selfrag_*` + `__post_init__` validation (max_iterations>=1 is the RESILIENT bound). **Mutation-proven:** corrective `retrieve_fn` bypassing the session pre-filter → denied chunk surfaces; circuit-breaker removed → grader-raise crashes the request; iteration cap bypassed → unbounded. make ci 425/0-skip; 210-cell oracle + leak demos + ReBAC parity + cache permission green. | live LLM grader (Groq/NVIDIA) gated; FakeGrader + injected retrieve_fn always tested | L |
| **P0.9** CDC/streaming + re-embedding (GOVERNANCE PROPAGATION) | **DONE** — a data change never leaves stale authorization reachable, fail-closed. Three sub-invariants, each mutation-proven: **(1) DELETE** — `store.delete_chunk` (row + links edges) + `cache.invalidate_chunk` → un-retrievable in all 3 modes + dropped from allowlist + cache-evicted; **(2) RECLASSIFICATION (sharp)** — B→F via CDC re-derives the allowlist from the live store row (under-cleared now DENIED) AND invalidates the stale permissive cache entry (a re-query MISSES, never serves now-restricted content); **(3) RE-EMBED** — `cdc.reembed.ReEmbedder` changes only vec+embedder_version, preserving cls/level/roles byte-identical (governance decision unchanged) + `invalidate_version`. `cdc/`: `events.ChunkChangeEvent` (op + monotonic source_version), `processor.CdcProcessor` (applies each event to store+cache; **version-ordered** = stale/replayed event dropped → idempotent + ordering-safe; **RESILIENT** = bounded retry → audited `cdc_dropped`, never silent; **TRANSPARENT** = span+audit per event), `reembed.ReEmbedder` (resumable, batched). + `store.delete_chunk`/`chunks_by_version`, `cache.invalidate_chunk` (both stores). **Mutation-proven:** skip delete/cache-invalidate → still reachable/stale-hit; skip reclassify-cache-invalidate → stale permissive entry served; drop cls on re-embed → governance changes. make ci 459/0-skip; 210-cell oracle + leak demos + ReBAC parity + cache permission + selfrag governance green. | live CDC source/stream + live embedder gated; in-mem/fixture CDC + deterministic embedder + SurrealDB store always tested | L |
| **P0.10** Multimodal (GOVERNED IDENTICALLY) | **DONE** — non-text chunks (image/table/OCR/figure) governed by the SAME pipeline as text, fail-closed. Three sub-invariants, each mutation-proven: **(1) same pre-filter** — a denied image/table is never a candidate in any mode (allowlist is modality-agnostic; cross-modal retrieval too); **(2) mask = WITHHOLD** — can't `[REDACTED]` a pixel inline, so `redact_chunk` for a non-text mask/partial strips the raw payload from metadata + uses a modality-aware withhold token (without it, copying metadata leaked raw bytes past a mask); **(3) extraction inherits classification** — OCR/extracted chunks carry the SOURCE doc's SecurityContext verbatim (class-F scan → class-F OCR, denied to under-cleared; NO declassify-by-extraction). `multimodal/`: `payload` (modality tag on EnrichedChunk + MULTIMODAL_PAYLOAD_KEY + attach/get/strip), `extract` (ModalityExtractor Protocol + Fake image/table/ocr extractors, inheritance-faithful), `embed.FakeMultimodalEmbedder` (text+caption share one space → cross-modal; embeds text surface not raw bytes → no payload-in-index), `robust` (guarded_extract: extractor crash → audited not silent = RESILIENT; payload size bound = ELASTIC). **Mutation-proven:** skip payload-strip → raw image/table/OCR surfaces; extractor mints public context → under-cleared sees restricted extraction. make ci 493/0-skip; 210-cell oracle + leak demos + ReBAC parity + cache permission + selfrag governance + CDC propagation green. | live multimodal embedder (Voyage-mm/CLIP) + live OCR gated; Fake extractors/embedder + fixtures always tested | M |
| **P0.11** Final: integration wiring + demo + deploy + docs | **P0.11a INTEGRATION WIRING DONE** — every built-standalone component wired into the live `RAGPipeline` as optional injected collaborators (defaults preserve behavior; 210-cell oracle stays green): W1 Tracer + Langfuse attribute allowlist (metric-only, fail-closed), W2 SemanticCache with the REAL govern_fn (re-run SecurityFilter, not identity), W3 CorrectiveLoop with the real session-scoped retrieve_fn (loop self-redacts), W4 mode-router + AllowlistBackend (in_process default, ReBAC parity), W5 text-to-SQL + GuardedBigQuery (mask flag governance-derived, ColumnPolicy from catalog, result rows masked by AST source), W6 multimodal guarded_extract into ingest (inherited SecurityContext, oversize/crash audited), W7 CdcProcessor + embedder_version (reclassify-up → deny + cache-invalidate end-to-end). **W8 MASTER END-TO-END LEAK ORACLE: the 210-cell oracle passes through the FULLY-WIRED pipeline (cache+loop+tracer all live); mutation-proven — disable ANY component's governance (L5 filter, loop self-redact) → the oracle leaks.** make ci 527/0-skip. Remaining: P0.11b demo surface, P0.11c LIVE deploy (gate removed), P0.11d top-down docs. | each W mutation-proven through the live pipeline; live LLM/embedder paths still gated | M |

## Dependency graph

```
S1 ─► P0.1a ─► P0.1b ─┬─► P0.2a ─► P0.2b ─► P0.2c ─► P0.2d ─┐
                      └─► P0.3a ─► P0.3b ─────────────────┤
                                                          ├─► P0.4 ─► P0.5 ─► P0.6 ─► P0.7 ─► P0.8 ─► P0.9 ─► P0.10 ─► P0.11
                      (P0.2a ∥ P0.3a parallel; both need only P0.1b)
                      (P0.2c wired P0.2b's index+pre-filter into live retrieval — DONE)
                      (P0.2d: store-backed ChunkSource adapter + O(N) allowlist fix; before/with P0.3b)
```

## Spikes
- **S1 TurboVec-in-serverless** ✅ done — KEEP (`docs/spikes/s1-turbovec-serverless.md`).
- **S2 SurrealDB 4-mode** ✅ done — KEEP (`docs/spikes/s2-surrealdb-four-mode.md`).
- **S3 Wren-AI-vs-glossary** — first task inside P0.4 (decision pending — default: keep the first-party build).
- **S4 BigQuery cost guard** — first task inside P0.3a.

## Tracked follow-ons (not yet built — explicit so they aren't lost)
- **Recall on REAL Voyage embeddings (not the CI HashingEmbedder):** P0.2b re-validated recall on clustered + HashingEmbedder vectors (honest ~0.45 tight / ~0.90+ text; reranker recovers). The production embedder is Voyage-3-large — recall on *real Voyage* embeddings is a **creds-gated** validation to run when a Voyage key is available (the offline build path). Until then the reranker-load-bearing finding stands. → fold into P0.2b-followup / the offline-build phase.
- **Adaptive router:** still the regex `HeuristicRouter`. **semantic-router** (spec §9, MIT, local, embedding-similarity) replaces it on the vector path. → P0.3b ("router learns indexable-derivative vs PB-tail") is the natural home; adopt semantic-router there.
- **ReBAC sub-linear allowlist:** ✅ DONE (P0.6) — `AllowlistBackend` ABC; `InProcessAllowlist` default + `LocalReBACAllowlist` (zero-dep tuple kernel) + `SpiceDBAllowlist` (authzed, gated) + `OsoCloudAllowlist` (gated); list-objects sub-linear lookup, **parity-guarded** against the in-process decision so the optimization never changes allow/deny. (Live SpiceDB-binary + Oso-Cloud integration runs are gated; a `spicedb serve` smoke under `@pytest.mark.spicedb` is the one residual live-path follow-on.)
- **Malformed-row audit:** ✅ DONE (P0.5) — durable AuditSink (InMemory + SurrealAuditSink) wired into chunk_source; malformed-row drops are queryable governance records, not just logs.
- **Index-level pre-filter for graph/SQL/lexical modes:** ✅ DONE (P0.6) — all three non-vector modes now DROP unauthorized content at the query (`id IN $allow`), not post-hoc; hardened + mutation-proven per mode. (Wiring these three retrievers into the live `RAGPipeline` mode-router is the integration follow-on, alongside the text-to-SQL + Tracer pipeline-wiring items below.)
- **Wire text-to-SQL agent + GuardedBigQuery into the live pipeline/router** (P0.3b governance advisory): the agent is built standalone (like GuardedBigQuery, the TurboVecRetriever before P0.2c). Integration: route warehouse-SQL queries through `TextToSqlAgent` from `RAGPipeline`; derive the `mask` flag from the real `GovernanceDecision` over the warehouse asset (session-derived, NOT a literal bool); source the `ColumnPolicy` set from the catalog asset (single governance source, not the agent's `masked_columns` kwarg). → integration/P0.6-era work, pairs with the other pipeline-wiring follow-ons.
- **Wire the Tracer into the live pipeline + Langfuse attribute allowlist** (P0.5 follow-on): the `Tracer` is a facade with 0 `.span()` calls in `src/` today. Integration: add spans around route/retrieve/rerank/govern/generate in `RAGPipeline`, AND a **Langfuse span-attribute ALLOWLIST** (only metric keys like cost_usd/tokens/mode/request_id exported) so a careless `span("generate", answer=...)` can never leak chunk content to Langfuse Cloud. -> integration/P0.6-era work.
- **Wire the SemanticCache into the live `RAGPipeline.query`** (P0.7 follow-on): the cache is built+hardened standalone (like the retrievers/Tracer were). Integration: at `query()` entry, when `cfg.cache_enabled`, `cache.get(question, session, govern_fn)` where `govern_fn` re-runs the L5 `SecurityFilter` over the stored chunk ids for the session; on miss, run the pipeline then `cache.put(...)`. Emit `record_cache_hit(...)` on a hit. Choose the backend from `cfg.cache_backend` (memory|surreal). The permission-aware invariant is already enforced by the cache; wiring must pass a govern_fn that is the REAL governance (not identity). → integration/P0.6-era work, pairs with the other pipeline-wiring follow-ons.
- **Cache invalidation on re-embedding/version bump** (P0.7 → P0.9 CDC): ✅ DONE (P0.9) — `ReEmbedder.reembed_all` calls `cache.invalidate_version(old)` on completion; `CdcProcessor` calls `cache.invalidate_chunk` on every event. The remaining wiring is connecting `CdcProcessor` to a LIVE change stream (the standalone in-memory/fixture-driven processor is built+tested) — see the live-CDC-source follow-on below.
- **Live CDC source + wire CdcProcessor to a real change stream** (P0.9 follow-on): the `CdcProcessor` is built+hardened against an injected event sequence (the in-memory/fixture path). Integration: a source connector (SurrealDB LIVE SELECT / a CDC log / a polling differ) emits `ChunkChangeEvent`s with monotonic `source_version`; feed them to `CdcProcessor.apply`. **HARD REQUIREMENT on the connector:** `source_version` MUST be unique-and-strictly-increasing PER EVENT for a chunk (a source LSN / commit timestamp / sequence), NOT per logical state — the processor's `<=`-drop ordering guard means two different changes sharing a version would silently drop the second (and a same-version reclassify-up would leave permissive state live). Wire the re-embed `embedder_version` into the live query path so queries select the matching version. → integration work, creds/stream-gated; pairs with the cache + loop + text-to-SQL + Tracer wiring follow-ons.
- **Live multimodal embedder + OCR + wire extractors into ingest** (P0.10 follow-on): the modality extractors + `FakeMultimodalEmbedder` are built+hardened (governance is the same regardless of embedder). Integration: a live multimodal embedder (Voyage-multimodal / CLIP-style, which embeds raw pixels+text jointly) + a live OCR engine (Tesseract/cloud) behind the existing `Embedder` / `ModalityExtractor` ABCs, creds/binary-gated; wire `guarded_extract` over the modality extractors into the ingest pipeline (so a real image/table/scanned-PDF produces governed chunks). **The governance invariant is already enforced regardless of embedder** — a live multimodal embedder must NOT bypass the allowlist pre-filter, and `redact_chunk`/extractor-inheritance hold for any embedder. → integration work, creds-gated; pairs with the other wiring follow-ons.
- **Wire the CorrectiveLoop into `RAGPipeline.query`** (P0.8 follow-on): the loop is built+hardened standalone. Integration: when `cfg.selfrag_enabled`, run `CorrectiveLoop(retrieve_fn=<the REAL session-scoped retriever, e.g. TurboVecRetriever.retrieve_for_session / P0.6 mode retrievers>, generator=<real generator>, grader=<from cfg.selfrag_grader>, max_iterations=cfg.selfrag_max_iterations).run(question, session)`; surface `LoopResult.abstained`/`stop_reason` on `RAGResponse`; emit the per-iteration spans/funnel. **The govern-preservation is guaranteed by passing the real session-scoped retrieve_fn — wiring must NOT pass a permission-blind retriever.** The loop now ALSO self-applies `SecurityFilter.apply` after each retrieve (redacts mask/partial -> [REDACTED]) as defense-in-depth, so even a misconfigured retrieve_fn can't surface raw mask-tier PII; this is idempotent with the pipeline's stage-5 redaction (double-redaction is harmless). Add an integration test routing a corrective loop through the REAL session-scoped retriever asserting no denied chunk surfaces across iterations end-to-end. → integration/P0.6-era work, pairs with the cache + text-to-SQL + Tracer wiring follow-ons.
- **Broaden view-miner/glossary coverage** (P0.4 finding): the view-miner only mines *aliased view columns*, so the drift-clean claim is scoped to the **mined set** (documented in `test_glossary_drift_clean_against_mined_columns`), not the full physical estate. Broaden coverage (mine query logs / dbt manifests / profile-then-draft every physical column) so drift-clean spans the full `legacy_mart` schema. → P0.4 follow-on.

## Workflow
Git worktree per phase; executing-plans in batches; **direct review between batches + Friday's independent review fan-out at each phase completion**; finishing-a-development-branch at end. Push branches + `main` to **GitHub** (Gitea skipped while titan-pc down). **Per-phase CEO pause WAIVED for the autonomous completion run** (CEO directive 2026-06-09) — quality gates stay; stop only for genuine blockers (missing creds where a phase's CORE needs them, infra down).
